import type { LocalDate, MonthKey } from "../dates";

export type Id = string;

/** ISO 4217 currency code, e.g. "USD", "AUD". */
export type CurrencyCode = string;

export type { LocalDate, MonthKey };
